let a=require('express'),b=a.Router(),{authenticateToken:c}=require('../middleware/auth'),d=require('../controllers/dashboardController'),{validateProfileUpdate:e,validatePasswordChange:f}=require('../middleware/validation'),{profileUpload:g}=require('../middleware/upload'),h=g;b.use(c);b.get('/',d.getDashboard);b.get('/profile',d.getProfile);b.post('/profile',h.single('profileImage'),e,d.updateProfile);b.get('/my-campaigns',d.getMyCampaigns);b.get('/my-donations',d.getMyDonations);b.get('/saved',d.getSavedCampaigns);b.get('/settings',d.getSettings);b.post('/settings/password',f,d.changePassword);b.delete('/settings/account',d.deleteAccount);module.exports=b;
